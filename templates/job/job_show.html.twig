{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <h2>{{ job.title }}</h2>
            <p>{{ job.description }}</p>
        </div>
    </div>
{% endblock %}